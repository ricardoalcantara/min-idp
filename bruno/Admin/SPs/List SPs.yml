info:
  name: List SPs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/admin/sps"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
