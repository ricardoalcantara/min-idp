info:
  name: Login JWT
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "login": "admin",
        "password": "{{adminPassword}}"
      }

runtime:
  variables:
    - name: baseUrl
      value: https://min-idp.toquinha.online
  scripts:
    - type: after-response
      code: bru.setEnvVar("accessToken", res.body.access_token)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
