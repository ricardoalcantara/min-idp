info:
  name: Update SP
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/admin/sps/{{?spId}}"
  auth:
    type: bearer
    token: "{{sessionId}}"
  body:
    type: json
    data: |-
      {
        "name": "Updated Name",
        "enabled": true
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
