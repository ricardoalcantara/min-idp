info:
  name: List User Sessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/admin/users/{{?userId}}/sessions"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
