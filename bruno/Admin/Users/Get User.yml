info:
  name: Get User
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/admin/users/{{?userId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
