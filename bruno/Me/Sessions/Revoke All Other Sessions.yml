info:
  name: Revoke All Other Sessions
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/me/sessions"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
