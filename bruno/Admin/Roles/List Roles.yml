info:
  name: List Roles
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/admin/roles"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
