info:
  name: Revoke All Sessions
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/api/me/sessions"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
