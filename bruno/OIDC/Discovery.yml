info:
  name: Discovery
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/.well-known/openid-configuration"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
