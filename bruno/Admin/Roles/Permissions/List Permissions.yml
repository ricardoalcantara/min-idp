info:
  name: List Permissions
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/admin/roles/{{?roleId}}/permissions"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
