info:
  name: Rotate Key
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/admin/keys/{{?protocol}}/rotate"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
