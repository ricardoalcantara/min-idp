info:
  name: Revoke Session
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/me/sessions/{{?sessionIdToRevoke}}"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
