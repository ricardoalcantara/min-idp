info:
  name: Delete SP
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/sps/{{?spId}}"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
