info:
  name: Get Group
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/admin/groups/{{?groupId}}"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
