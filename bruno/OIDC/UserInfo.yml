info:
  name: UserInfo
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/oauth2/userinfo"
  auth: none
  headers:
    - name: Authorization
      value: Bearer {{oidcAccessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
