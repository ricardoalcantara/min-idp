info:
  name: Delete Role
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/roles/{{?roleId}}"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
