info:
  name: Remove Group
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/users/{{?userId}}/groups/{{?groupId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
