info:
  name: Remove Role
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/users/{{?userId}}/roles/{{?roleId}}"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
