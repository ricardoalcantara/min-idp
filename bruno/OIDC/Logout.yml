info:
  name: Logout
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/oauth2/logout?post_logout_redirect_uri={{oidcRedirectUri}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
