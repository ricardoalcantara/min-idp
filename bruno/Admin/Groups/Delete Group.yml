info:
  name: Delete Group
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/groups/{{?groupId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
