info:
  name: Get Me
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/me"
  auth:
    type: bearer
    token: "{{sessionId}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
