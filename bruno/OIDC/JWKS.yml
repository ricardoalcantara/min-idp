info:
  name: JWKS
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/.well-known/jwks.json"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
