info:
  name: Authorize
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/oauth2/authorize?client_id={{oidcClientId}}&redirect_uri={{oidcRedirectUri}}&response_type=code&scope=openid&state=123"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
