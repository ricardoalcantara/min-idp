info:
  name: Get OIDC Config
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/admin/sps/{{?spId}}/oidc"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
