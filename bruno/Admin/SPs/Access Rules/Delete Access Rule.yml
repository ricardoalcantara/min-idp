info:
  name: Delete Access Rule
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/admin/sps/{{?spId}}/access-rules/{{?ruleId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
