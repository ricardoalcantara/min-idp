info:
  name: Update User
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/api/admin/users/{{?userId}}"
  auth:
    type: bearer
    token: "{{sessionId}}"
  body:
    type: json
    data: |-
      {
        "status": "disabled"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
